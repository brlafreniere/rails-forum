<% if current_user.admin? %>
    <%= link_to "Create Topic", new_topic_path, class: "btn btn-primary mb-3" %>
<% end %>

<table class="table table-bordered">
    <% @topics.each do |topic| %>
        <tr>
            <td>
                <%= link_to topic.name, topic_path(topic) %>
            </td>
        </tr>
    <% end %>
</table>