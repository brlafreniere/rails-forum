<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container">
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#toggleMenu" aria-controls="navbarTogglerDemo01" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="toggleMenu">
            <a class="navbar-brand" href="/">Rails Forum</a>
            <ul class="navbar-nav mr-auto">
            </ul>
            <ul class="navbar-nav">
                <% if current_user.admin? %>
                    <li class="nav-item">
                        <%= link_to "Admin Dashboard", admin_dashboard_path(), class: "nav-link" %>
                    </li>
                <% end %>
                <li class="nav-item">
                    <%= link_to "Sign Out", destroy_user_session_path(), method: :delete, class: "nav-link" %>
                </li>
            </ul>
        </div>
    </div>
</nav>